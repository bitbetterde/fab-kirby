type: files
label: Aufnahmen
layout: table
image: false
text: "{{ file.title }}"
info: "{{ file.created.toDate('d.m.Y H:i') }}"
sortBy: created desc
icon: key
image: true
columns:
  isPublished:
    label: Veröffentlicht
    type: text
    value: "{{ file.published ? 'Ja' : 'Nein' }}"
template: recording
empty: "Es gibt noch keine Aufnahmen."