title: Vertical News Card Slider
preview: fields
fields:
  mode:
    label: Modus
    type: radio
    options:
      manual: Mehrere Seiten Manuell wählen
      children: Seite wählen und dessen Unterseiten verwenden
    default: manual
  title:
    label: Titel
    type: text
    required: false
  pages:
    label: Seiten wählen
    type: pages
    multiple: true
    query: site.pages
    when:
      mode: manual
  parent:
    label: Seite wählen
    type: pages
    multiple: false
    query: site.pages
    when:
      mode: children
  fullbleed:
    label: Full Bleed
    type: toggle
    default: false
