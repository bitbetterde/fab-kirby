type: group
fields:
  source:
    label: Bildquelle
    type: link
    required: true
    options:
      - file
      - url

  alt:
    label: field.blocks.image.alt
    type: text
    icon: title

  caption:
    label: field.blocks.image.caption
    type: writer
    icon: text
    inline: true

  subCaption:
    label: Quelle
    type: url

  tag:
    label: Overlay
    type: text
    icon: tag
